app:
  customer:
    client:
      baseUrl: http://customer-api
management:
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  server:
    port: 8081
spring:
  application:
    name: init-microservice
  flyway:
    enabled: true
