spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
pactbroker:
  host: https://harmelodic.pactflow.io
  auth:
    username: ${PACT_BROKER_USERNAME:broker-user}
    password: ${PACT_BROKER_PASSWORD:broker-pass}
